# MANOJ TRACKING SYSTEM — environment template

# Copy this file to server/.env and fill in real values.
# server/.env is git-ignored — never commit it.
# See server/.env.example for the full list of variables.